const config = {
    src : {
        root   : './src',
        scripts: './src/js',
    },
    dist: {
        root   : './build',
        scripts: './build/js',
        zip    : './build/zip'
    }
};

export default config;
